rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Rules for the 'alerts' collection
    match /alerts/{alertId} {
      // ANYONE can read ('get') a single alert document or list the entire collection.
      allow read: if true;

      // ONLY an authenticated user can create ('write') an alert.
      // Data must be valid according to the specified constraints.
      allow create: if request.auth != null &&
                      request.resource.data.message is string &&
                      request.resource.data.message.size() >= 5 &&
                      request.resource.data.message.size() <= 140 &&
                      request.resource.data.type is string &&
                      request.resource.data.type in ['Traffic', 'Weather', 'Accident', 'Road Hazard', 'Collision'] &&
                      request.resource.data.timestamp == request.time;

      // NO ONE can update or delete alerts from the client.
      allow update, delete: if false;
    }
  }
}