rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /alerts/{alertId} {
      allow read: if true;
      allow create: if request.auth != null;
    }
    match /sent-alerts/{sentAlertId} {
      allow create: if request.auth != null;
    }
  }
}
